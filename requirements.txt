langchain==1.2.14
langchain-text-splitters==1.1.1  # token-based chunking (Q1: from_tiktoken_encoder)
langchain-openai==1.1.12
langchain-pinecone==0.2.13
pinecone==7.3.0                # A2: replaces deprecated pinecone-client (langchain-pinecone uses the pinecone pkg)
langchain-experimental==0.4.1

# Document parsing (B2: lightweight; replaced unstructured[all-docs] + its ML tail)
pymupdf==1.27.2.3             # PDF text + image extraction
python-docx==1.2.0           # DOCX text + image extraction

streamlit==1.56.0
python-dotenv==1.2.2
ragas==0.4.3

fastapi==0.135.2
uvicorn[standard]==0.42.0
python-multipart==0.0.22
pydantic[email]==2.12.5
pydantic-settings==2.13.1       # Part C: typed settings + fail-fast secret validation (A10)

supabase==2.28.3
httpx==0.28.1
tiktoken==0.12.0
aiofiles==25.1.0

# Observability
langsmith==0.7.23              # LangSmith tracing (LangChain-native, env-driven; O1)

# Caching
redis==8.0.1                   # exact-match query cache (C1); fakeredis is used in tests

# Rate limiting & async file I/O
slowapi==0.1.9

# RAG Quality Features
# Feature A (hybrid) is Pinecone-native sparse+dense now (L1); the sparse encoder
# is hand-rolled in src/components/sparse.py (stdlib only — pinecone-text's mmh3
# has no py3.13 wheel), so no extra dep.
cohere==7.0.4                  # Hosted Rerank API (Feature B / L2; replaces local cross-encoder + torch)

-e .
